[Your Local Machine] ---> Uploads MultiCharts_Results.csv & PowerLanguage.txt ---> [RunPod Jupyter Server]|[RunPod Jupyter Kernel] <--- Executes Scikit-Learn Script Locally <--------------------------+|v Generates: Feature Importances & Parameter Boundaries|[DeepSeek-R1 / LLM Engine] <--- Fed with Code Rules, Metadata Headers & Summary Logs (No Data Bloat)|v Outputs: Structural Edge Assessments & Curve-Fitting Verification Report

### Execution Protocol

1.  **File Deployment**: Access the Jupyter File Manager via your custom RunPod proxy link. Securely drag and drop your raw strategy `code.txt` and your target `MultiCharts_Results.csv` file into the active workspace directory.
2.  **Data Pruning via Python Engine**: Run the `multicharts_ml_optimizer.py` file inside your terminal or Jupyter notebook instance. This local python process crunches the 2000 rows instantly using Scikit-Learn without expanding token usage.
3.  **Prompt Construction**: Instead of loading the massive raw 2,000-row text block directly to the neural layers, read the output logs generated by the script (`llm_payload_summary.csv` or standard out text tree), copy them, and interface with your local model using this structured system framework:

### The Production Prompt Framework

```text
You are operating as an isolated Quantitative Risk Engineer specializing in MultiCharts PowerLanguage architectures.

I have deployed a machine learning model locally over my optimization matrix consisting of 2,000 runs across 52 columns. Below are the structural code files and the resulting Scikit-Learn tree logic output.

CROSS-REFERENCE AND ANALYZE:
1. Review the PowerLanguage source code mechanics. Determine if it is a trend-following or mean-reverting script.
2. Map the Machine Learning decision tree output back to the code logic.
3. Isolate if the optimal parameters suggested by Scikit-Learn represent a valid trading edge or if they fall into fragile, over-optimized parameter "cliffs".

--- START POWERLANGUAGE SOURCE CODE ---
[Paste your strategy script text here]
--- END POWERLANGUAGE SOURCE CODE ---

--- START LOCAL SCIKIT-LEARN DECISION TREE LOGS ---
[Paste the text output rules & Top 5 matrix output by your script here]
--- END LOCAL SCIKIT-LEARN DECISION TREE LOGS ---
```

---

## 4. Analytical Advantages

*   **Zero Token Bloat**: Sending raw structural logs rather than full data tables saves thousands of tokens per turn, keeping your context snappy and precise.
*   **Exact Mathematics**: By offloading statistical modeling to `scikit-learn`, your LLM acts strictly as a structural logic engineer rather than a noisy calculator, removing calculations-based hallucination errors entirely.
Would you like to look over a specific PowerLanguage script snippet together so we can configure the matching header strings for the INPUT_PARAMETERS array inside the script?